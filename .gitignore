node_modules/
build/
dist/
coverage/
.DS_Store
*.log
gha-creds-*.json
.env*
!.env.example
.claude/
.worktrees/
.superpowers/
*-firebase-adminsdk-*.json

# Design sources: originals kept out of the deploy
design-sources/*
# ...except the cut-outs the email art is built from. Those are inputs to
# `npm run digest:art`, not reference material, and a build whose source is
# only on one laptop is a build that cannot be repeated.
!design-sources/email-cutouts/

# Digest email preview output (npm run digest:preview)
dist-preview/
functions/assets/
